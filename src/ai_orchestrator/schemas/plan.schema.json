{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ai-orchestrator.dev/schemas/plan.schema.json",
  "title": "Plan",
  "description": "A natural implementation plan produced by the planning phase.",
  "type": "object",
  "required": ["plan_id", "task", "approach", "implementation_steps", "key_files"],
  "additionalProperties": false,
  "properties": {
    "plan_id": {
      "type": "string",
      "pattern": "^[0-9a-f]{8}(-[0-9a-f]{4}){3}-[0-9a-f]{12}$",
      "description": "UUID v4 identifier for this plan."
    },
    "task": {
      "type": "string",
      "minLength": 1,
      "description": "The task description this plan implements."
    },
    "approach": {
      "type": "string",
      "minLength": 1,
      "description": "Reasoning, strategy, and important constraints for the implementation."
    },
    "implementation_steps": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Ordered natural-language implementation steps."
    },
    "key_files": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^(?!/)(?!.*\\.\\./)"
      },
      "description": "Flat list of relevant repository-relative paths. Paths must not contain .. segments or start with /."
    }
  }
}
